/**
 * Default temporary password for client (investor) users.
 * Clients can log in with this password and change it from Settings.
 */
export const CLIENT_TEMP_PASSWORD = 'SamavedaCapital#Client1234';
